# ローカル開発用: docker compose up → http://localhost:8080
services:
  nginx:
    build:
      context: ./nginx
    ports:
      - "8080:80"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost/"]
      interval: 30s
      timeout: 3s
      retries: 3
